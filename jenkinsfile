pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                script {
                    // Call the deployToEnvironment function with the input string
                    data = load 'file.groovy'
                    data.deployToEnvironment('PROD')
                }
            }
        }
    }
}
